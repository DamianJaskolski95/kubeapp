apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: fastapikube-project
  labels:
    app: fastapikube
spec:
  type: NodePort
  selector:
    app: fastapikube-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30009